# Frontend (React + Vite)

## Commands
```bash
npm install
npm run dev
npm run build
npm run preview
```

## Notes
- The app expects the Flask API at http://localhost:5000.
- You can set `VITE_API_BASE` to override.
