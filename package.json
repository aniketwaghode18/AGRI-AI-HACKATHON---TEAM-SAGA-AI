{
  "name": "agrivision-root",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "frontend": "npm --prefix src/frontend run dev",
    "frontend:install": "npm --prefix src/frontend install",
    "api": "python src/server/app.py"
  }
}
